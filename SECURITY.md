# 安全策略

## 上报漏洞

**请不要用公开 issue 上报安全漏洞。** 公开 issue 在修复发布前就把问题暴露给了所有人。

请用 GitHub 的私密上报通道：
**[Security → Report a vulnerability](https://github.com/njfuzrs/claude-trace/security/advisories/new)**

走这个入口而不是邮箱，是因为它对双方都更省事：上报内容在草案阶段只有你和维护者可见，
修复过程、CVE 申请与最终公开都在同一个页面里，不需要交换任何邮箱地址。

上报时尽量包含：

- 漏洞类型与影响（能读到什么、能执行什么、需要什么前置条件）
- 复现步骤，或一个最小复现用例
- 受影响的版本（仓库根目录 `version` 文件的内容）与 macOS 版本
- 你认为的严重程度，以及是否已在野外被利用

我们会在**5 个工作日内**回复确认收到。修复后会在 `CHANGELOG.md` 里记录，
如果你愿意署名，我们会在致谢里写上你（默认不写，除非你明确说可以）。

**本项目是个人项目，不是有 SLA 的商业产品**，没有安全响应团队，也没有漏洞赏金。
响应速度取决于维护者的可用时间 —— 这一点如实说明，不做承诺。

## 支持的版本

只支持**最新版本**。旧版本不做安全回溯。

---

## 🔴 本工具的隐私边界（使用前必读）

claude-trace 是一个**采集工具**。它的全部价值来自记录完整交互，
所以它天然持有你最敏感的开发数据。这一节如实说明边界在哪。

### 采集范围

落盘的是**完整对话**，不是摘要：

| 内容 | 说明 |
| --- | --- |
| 完整请求体 | 每一条提示词、system prompt、全部历史消息 |
| 完整响应体 | 模型回复，含 thinking 内容与 token 用量 |
| 工具调用与结果 | `Read` / `Edit` / `Bash` 的入参与输出 —— 即**你的源码内容、命令与命令输出** |
| 文件路径 | 绝对路径，含你的用户名目录 |
| git 状态 | 会话起点的 HEAD、分支名、工作区是否有未提交改动（只读命令） |

### 脱敏覆盖面（如实描述，不夸大）

- **已脱敏**：请求头中的 `x-api-key`、`authorization`、`proxy-authorization`
  三个字段，落盘时截断为前 10 字符 + `***`。
  实现见 `proxy.py` 的 `SENSITIVE_HEADERS` 与 `sanitize_headers_for_storage()`，
  对应测试在 `tests/test_sanitize.py`。
- 🔴 **未脱敏**：**消息体不做任何内容级过滤**。你在对话里粘贴过的 token、私钥、
  `.env` 内容、数据库连接串，都会**明文落盘**在 `raw.jsonl` / `session.traj` 中。
- 内容级脱敏器（Scrubber）尚未实现，在路线图上。**在它落地之前，
  请把轨迹目录当作与源码同等敏感的数据。**

### 上传默认关闭

本项目**不含任何内置上传端点或内置凭据**。上传是 opt-in 的，
需同时配置 `TRAJ_PLATFORM_URL` 与 `TRAJ_UPLOAD_TOKEN` 才启用，任一为空即禁用。
`TRAJ_USER_ID` / `TRAJ_DEVICE_ID` 默认留空，**不会回退到你的系统用户名或主机名**。

自建接收端的协议见 `docs/upload-protocol.md`。⚠️ 若你的接收端是明文 HTTP，
轨迹会在链路上明文传输 —— 请用 HTTPS。

---

## 这个项目的攻击面（上报前值得知道）

claude-trace 是一个**在你本机上做 HTTP 中间人、并把流经的数据全量落盘**的工具。
它的设计前提是：**用户主动选择采集自己的数据，并对这些数据有最终控制权**。
所以下面这些**不算漏洞**：

- **它记录了完整对话内容** —— 这是它的功能，不是泄露。
- **消息体里的密钥没被脱敏** —— 已在上面如实声明，属于已知现状（Scrubber 未实现），
  不需要重复上报「我发现 .env 内容进了 traj」。但如果你发现**请求头脱敏**被绕过，请上报。
- **配置了上传后数据发到了你填的服务器** —— 那是你填的端点。
- **`channels.json` 里有你的明文 API token** —— 该文件在 `.gitignore` 中，
  信任边界在本机文件系统权限那一侧。

下面这些**算漏洞，请上报**：

- **请求头脱敏被绕过**：构造出让 `x-api-key` / `authorization` 明文落盘的情形
  （例如大小写、重复头、非标准头名导致 `SENSITIVE_HEADERS` 匹配失效）。
- **代理监听面扩大**：默认应只绑 `127.0.0.1`，任何导致监听到 `0.0.0.0` 或
  局域网可访问的路径 —— 这会让同网段的人读到你的全部对话。
- **路径遍历**：`session_id` 等外部可控值导致写入轨迹目录之外
  （已有 `_SAFE_SESSION_ID_RE` 防护与 `tests/test_session_id.py` 锁定，绕过它请上报）。
- **凭据泄露到非预期位置**：token 出现在日志、错误信息、`.uploaded` 标记文件，
  或被发到用户没有配置的第三方端点。
- **上传链路问题**：`TRAJ_UPLOAD_TOKEN` 泄露、SHA256 校验可绕过、
  或在两个变量均为空时仍然发生了外发请求。
- **供应链问题**：GitHub Releases 产物的完整性、`dist/install.sh` 的下载校验。

## 你自己使用时的建议

- **不要在私有代码库上无脑开采集**。先看一眼 `./trajectories/sessions/` 里到底落了什么。
- **分享或上传轨迹前自查凭据**：

  ```bash
  grep -rlnE 'sk-[A-Za-z0-9_-]{20,}|ghp_[A-Za-z0-9]{12,}|AKIA[A-Z0-9]{12,}|BEGIN [A-Z ]*PRIVATE KEY' \
    ./trajectories/sessions/ 2>/dev/null
  ```

- **轨迹目录不要进版本控制**。`.gitignore` 已排除 `trajectories/` 与 `*.traj`，
  但那只在你用默认路径时有效；自定义 `$OUTPUT` 后请自己确认。
- **上传端点用 HTTPS**，并给它做来源 IP 限制。
